import 'package:flutter/material.dart';

class SODDetailsPage extends StatelessWidget {
  const SODDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
